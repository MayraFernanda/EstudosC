#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*
        51.	Faça uma função em C que, recebendo como parâmetro dois números inteiros,
        resulta o resto da divisão inteira deles (o resto da divisão inteira do primeiro deles pelo segundo).

        Importante lembrar que os números com os quais vocês trabalharão são inteiros, logo, podem ser positivos
        ou negativos. Fica proibido declarar variáveis, fazer uso de operadores aritméticos (+, -, *, / e %),
        dos operadores relacionais (!=, <, <=, > e >=), de operadores lógicos (!, && e ||), dos comandos de seleção
        (if com else e switch), de comandos repetitivos (while, do-while e for), de funções auxiliares e de qualquer
        função de biblioteca. Dica: use ++, --, if sem else, return, recursão e as funções feitas a partir do exercício 22.
        Faça um programa em C para testar sua função.

    */

    printf("Insira o primeiro inteiro");
    scanf("%d", &a);
    printf("Insira o segundo inteiro");
    scanf("%d", &b);


}
