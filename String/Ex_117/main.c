#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    /*
        117.Escreva uma função em C que resulte uma cadeia de caracteres em tudo
        idêntica a uma cadeia de caracteres fornecida, exceto pelo fato de que nela
        todas as ocorrências de uma subcadeia também fornecida teriam sido removidas.
        Faça um programa em C para testar sua função.
    */

    scanf("%s", str);
    int len = strlen(str);
    printf("%d", len);

    char[] subString(char[] str, char[] s){
        char ret [100];

        int tamStr = strlen(str);
        for(i=0; i<tamStr; i++){
            //testar se é subString
        }
    }

    bool ehSubString(char[] str, char[] s, int i){
        ret = false;
        int tam = strlen(s);
        for(int j=0; j<tam; j++){
            if( )
        }
    }

    return 0;
}
